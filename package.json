{
  "name": "puyang.github.io",
  "private": true,
  "type": "module",
  "scripts": {
    "build:cv": "TMPDIR=$(mktemp -d) && trap 'rm -rf \"$TMPDIR\"' EXIT && latexmk -cd -xelatex -interaction=nonstopmode -output-directory=\"$TMPDIR\" cv/puyang-resume.tex && cp \"$TMPDIR/puyang-resume.pdf\" docs/public/cv/puyang-resume.pdf",
    "dev": "vitepress dev docs",
    "build": "vitepress build docs",
    "preview": "vitepress preview docs"
  },
  "devDependencies": {
    "markdown-it-mathjax3": "^4.3.2",
    "vitepress": "^1.6.4"
  }
}
